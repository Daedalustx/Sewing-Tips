<p id="notice"><%= notice %></p>

<p class="centered">
  <strong>Title:</strong>
  <%= @tip.title %>
</p>

<p class="centered">
  <strong>Headline:</strong>
  <%= @tip.headline %>
</p>

<div class="centered">
<p>
  <strong>Steps:</strong>
</p>
<ul>
  <% @tip.steps.each do |step| %>
  <li>
  <span><%= step.heading %> </span><br>
  <span><%= step.image %> </span><br>
  <span><%= step.content %> </span><br>
  <span><%= step.displayIndex %> </span><br>
  </li>
  <% end %>
</ul>
</div>

<p class="centered">
  <strong>Image:</strong>
  <%= @tip.image %>
</p>

<%= link_to 'Edit', edit_tip_path(@tip) %> |
<%= link_to 'Back', tips_path %>
